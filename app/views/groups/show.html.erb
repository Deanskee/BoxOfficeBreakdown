<h1>Groups#show</h1>
<p>Find me in app/views/groups/show.html.erb</p>
<div id= "profile-info">
<l1>Group Name: <%= @group.name %></li>
</br>
</br>
<li>Description: <%= @group.description %></li>
</br>
</div>
<div id="profile-photo">
        	<% if @user.avatar_file_name %>
        		<%= image_tag @user.avatar_url(:medium) %><br /><br />
        	<% end %>
        </div>

<li> Guess: <%# @guess.amount %> </li>

<% @group.users.each do |u| %>
Users in group: <%= u.name %>
Points: <%= u.points %>
<%= u.guesses %>
<% end %>

<%# @guess.users.each do |g| %>
	<%# g.guesses %>
<%# end %> 
<ul class="button-group">
<li><%= link_to 'delete', @group, class: "tiny button", method: :delete, data: {confirm: 'are you sure'} %></li>
<li><%= link_to 'edit', edit_group_path(@group), class: "tiny button" %></li>
</ul>