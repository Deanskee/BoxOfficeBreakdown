<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>BoxOffice Breakdown</title>
	
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>


	<div class="sticky">
      <nav class="top-bar" data-topbar>
        <ul class="title-area">
          <li class="name">
          </li>
        </ul>

        
        <section class="top-bar-section">
            <!-- Left Nav Section -->
          <ul class="left">
        <%= link_to "BoxOfficeBreakDown", root_path %>
   
           </ul>
			 
			  <ul class="right">
			  	<li><%= link_to "Add New Group", new_group_path %></li>
			  	<% if current_user || session[:remember_token] %>
			  	<li><%= image_tag current_user.profile_picture_url %></li>
			    <li class="has-dropdown">
			      <a href="#">Welcome <strong><%= current_user.name %></strong>! </a>
			    	<ul class="dropdown">	
			    	  <li><%=link_to "View", user_path(current_user) %></li>
                  	 
			   	 	  <li><%= link_to "Sign out", signout_path, id: "sign_out" %></li>
			   		</ul>
			    </li>
			</ul>
			  <% else %>
			    <li><%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %></li>
			<li><%= link_to "Sign Up", new_user_path %></li>
            <li><%= link_to "Sign In", new_session_path %></li>
			  <% end %>
            
		</section>
	  </nav>
    </div>

	 <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>

	  <%= yield %>

	<%= javascript_include_tag "application" %>
		    
  </body>
</html>
