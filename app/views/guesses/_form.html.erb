<%= form_for @guess do |f| %>

   <span class="title"> Guess Amount: <%= f.text_field :amount %>


  <%= f.collection_select :movie_id, Movie.all, :id, :title %>

  <%= f.submit "Submit", class: "tiny button" %>

<% end %>